# ── Core RAG stack ────────────────────────────
langchain>=0.3.0
langchain-text-splitters>=0.3.0
langchain-openai>=0.2.0
langchain-community>=0.3.0
langchain-qdrant>=0.2.0

# ── Vector store ──────────────────────────────
qdrant-client>=1.11.0

# ── Embeddings ────────────────────────────────
sentence-transformers>=3.3.0
FlagEmbedding>=1.3.0           # BGE-M3 FlagModel (faster batching)

# ── RAG Evaluation ────────────────────────────
ragas>=0.3.0
datasets>=3.0.0                # HuggingFace datasets
langchain-google-vertexai>=2.0.0  # required by ragas at import time

# ── LLM Observability ─────────────────────────
langfuse>=2.50.0

# ── Frontend ──────────────────────────────────
streamlit>=1.40.0

# ── Config & env ──────────────────────────────
pyyaml>=6.0
python-dotenv>=1.0.0

# ── Data handling ─────────────────────────────
pandas>=2.2.0
tqdm>=4.66.0

# ── HTTP client (for vLLM) ────────────────────
httpx>=0.27.0
openai>=1.50.0
langchain-text-splitters
langchain-google-vertexai
streamlit
pip install torchvision --index-url https://download.pytorch.org/whl/cpu
pip install httpx